package clases.UI;

public class Lanzador {
    public static void main(String[] args) {
            // Llamamos al main de la clase Login
            // Esto evita que el ejecutable falle al detectar la herencia de Application del login
            Login.main(args);
        }
    }